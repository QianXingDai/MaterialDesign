<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kakacat.materialdesign">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.bottomnavigation.IconActivity"></activity>
        <activity android:name=".activity.bottomnavigation.ShiftActivity" />
        <activity android:name=".activity.bottomnavigation.BasicActivity" />
        <activity android:name=".activity.ShowCodeActivity" />
        <activity android:name=".activity.bottomappbar.WithFabActivity" />
        <activity android:name=".activity.bottomappbar.BasicActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>